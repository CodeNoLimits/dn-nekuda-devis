# dn-nekuda-devis

DreamNova proposal — isolated **nekuda** site.

`VITE_SITE=nekuda` is baked in `.env.production`. This deployment never shows the other client or the agency hub.

Build: `npm run build` (Nitro Vercel preset).
